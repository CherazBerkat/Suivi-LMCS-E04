// Importing MovingBg and Cart_resetpassword components
import MovingBg from "../../components/login/MovingBg";
import Cart_resetpassword from "../../components/login/Cart_resetpassword";

// Reset_password component definition
const Reset_password = () => {
  // Rendering MovingBg and Cart_resetpassword components
  return (
    <div>
      <MovingBg />
      <Cart_resetpassword />
    </div>
  );
};

// Exporting Reset_password component
export default Reset_password;
